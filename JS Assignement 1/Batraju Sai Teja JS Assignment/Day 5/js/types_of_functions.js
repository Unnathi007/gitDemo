function square(num){
    console.log(`square of ${num} is ${num*num}`);
}

square(3)

let cube = function(num)
{
    console.log(`cube of ${num} is ${num*num*num}`);
}

cube(3)

let quadraple = (num) => console.log(`quadraple of ${num} is ${num*num*num*num}`);

quadraple(3)